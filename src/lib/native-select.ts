/**
 * Room for the custom chevron drawn in `globals.css` (`appearance: none` on native
 * selects). Use with `ps-*` instead of `px-*` on single-value `<select>`.
 */
export const nativeSelectTrailingPadding = "pe-10";
