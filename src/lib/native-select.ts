/**
 * Native `<select>` draws the dropdown indicator in the inline-end padding. Symmetric
 * `px-*` crowds the chevron against the border; pair `ps-*` with these instead.
 */
export const nativeSelectTrailingPadding = "pe-10";
